import { Card } from "../../components"
import { useEffect } from "react"

const HomeContainer = () => {
    useEffect(
    
        // function pertama anonymus function atau callback function
        () => {},

        // array of depedency
        []
    )

    return(
        <Card border>
            <div></div>
        </Card>
    )
}
export default HomeContainer